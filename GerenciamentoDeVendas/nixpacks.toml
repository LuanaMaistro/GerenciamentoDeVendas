[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["dotnet restore API/API.csproj -r linux-x64"]

[phases.build]
cmds = ["dotnet publish API/API.csproj -c Release -o out -r linux-x64 --self-contained true"]

[start]
cmd = "./out/API"
